# APK Downloads

Place the built APK file in this directory as `tyre-rescue-driver.apk`.

Build with:
```bash
cd driver-app
npx eas build --platform android --profile production --local
```

Then copy the generated `.apk` file here and rename it to `tyre-rescue-driver.apk`.
