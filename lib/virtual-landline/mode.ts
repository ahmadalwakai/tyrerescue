export const VIRTUAL_LANDLINE_PREVIEW_ONLY = false;

export const VIRTUAL_LANDLINE_PREVIEW_ONLY_CODE = 'VIRTUAL_LANDLINE_PREVIEW_ONLY';

export const VIRTUAL_LANDLINE_PREVIEW_ONLY_MESSAGE =
  'Virtual Landline is in Preview Mode until a real provider CSV has been validated. Import, database writes, booking drafts and notifications are disabled.';
